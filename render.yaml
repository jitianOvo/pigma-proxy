services:
  - type: web
    name: pigma-proxy
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: PROXY_TOKEN
        value: pigma-proxy-2026
